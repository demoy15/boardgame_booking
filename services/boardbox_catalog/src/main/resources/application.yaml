server:
  port: 8082
spring:
  application:
    name: catalog-svc
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/boardbox
    username: postgres
    password: postgres
  datasource:
    url: jdbc:postgresql://localhost:5433/boardbox
    username: postgres
    password: postgres
  sql:
    init:
      mode: always
  liquibase:
    enabled: false

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /docs
